{
  /* <Form onSubmit={handleSubmit}>
        <AmountLabel>
          Amount
          <input
            value={amount}
            name="amount"
            onChange={handleChange}
            type="number"
          />
        </AmountLabel>
        <ConvertLabel>
          From
          <SelectWrapper> */
}
{
  /* {firstFlag ? (
              <img
                src={`https://flagcdn.com/16x12/${firstFlag
                  .toLowerCase()
                  .trim()}.png`}
                // src={firstFlag}
                width="16"
                height="12"
                alt="Ukraine"
              />
            ) : (
              <BsFlag />
            )} */
}
{
  /* <div>
              <select
                defaultValue=""
                name="convertFrom"
                onChange={selectHandleChange}
              >
                <option value="" disabled>
                  --Choose currency --{" "}
                </option> */
}
{
  /* {currenciesCodes &&
                  currenciesCodes.map((code, index) => {
                    return (
                      <option value={code} key={index}>
                        {code}
                      </option>
                    );
                  })} */
}
{
  /* </select>
            </div>
          </SelectWrapper>
        </ConvertLabel>
        <ConvertLabel>
          To
          <SelectWrapper> */
}
{
  /* {secondFlag ? (
              <img
                src={`https://flagcdn.com/16x12/${secondFlag
                  .toLowerCase()
                  .trim()}.png`}
                alt=";"
              />
            ) : (
              <BsFlag />
            )} */
}
{
  /* <img src={secondFlag} width="16" height="12" alt="Ukraine" /> */
}
{
  /* <select
              defaultValue=""
              name="convertTo"
              onChange={selectHandleChange}
            >
              <option value="" disabled>
                --Choose currency --{" "}
              </option> */
}
{
  /* {currenciesCodes &&
                currenciesCodes.map((code, index) => {
                  return (
                    <option value={code} key={index}>
                      {code}
                    </option>
                  );
                })} */
}
{
  /* </select>
          </SelectWrapper>
        </ConvertLabel>
        <Button type="submit">Convert</Button>
        {status === Status.PENDING && <ColorRing />}
        {status === Status.RESOLVED && <p>{result}</p>}
        {status === Status.IDLE && <p>Enter amount and choose currency</p>}
      </Form> */
}
